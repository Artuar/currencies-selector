import * as React from "react";
import { hot } from "react-hot-loader";
import { CurrencySelector } from "app/components/CurrencySelector/CurrencySelector";

export const App = hot(module)(() => <CurrencySelector />);
